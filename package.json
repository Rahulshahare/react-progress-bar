{
  "name": "react-progress-bar",
  "version": "1.0.0",
  "description": "Component to generate a simple HX style loading bar (wrapped in a panel)",
  "main": "src/ProgressBar.jsx",
  "scripts": {
    "prestart": "ulimit -n 9999",
    "start": "watchify -t babelify doc/example.js -o doc/example-built.js -v",
    "test": "mocha --compilers js:babel/register test/test*",
    "lint": "make-up src test doc",
    "coverage": "istanbul cover _mocha -- --compilers js:babel/register test/test* && istanbul check-coverage --statements 100 --branches 100 --functions 100 --lines 100"
  },
  "author": "Rob Huzzey <robert.huzzey@holidayextras.com>",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/holidayextras/react-progress-bar.git"
  },
  "dependencies": {
    "react": "^0.13.3",
    "react-bootstrap": "^0.26.1"
  },
  "devDependencies": {
    "babel": "^5.8.23",
    "babelify": "^6.3.0",
    "browserify": "^11.1.0",
    "make-up": "^5.3.1",
    "mocha": "^2.3.3",
    "react-shallow-render": "^1.0.1",
    "watchify": "^3.4.0"
  }
}
